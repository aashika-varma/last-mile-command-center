# Roadmap

- [x] Landing page with agent roster + per-agent pages
- [x] Hero: US map with store locations (swap `STORE_LOCATIONS` in `src/lib/stores.ts` with real lat/lng)
- [x] Light theme + toggle in header (persisted)
- [ ] Wire agents to real data when backend is added
- [x] In-app content editor (/settings/content) for all copy, tab titles, metric labels, chart titles
- [x] Agent registry manager (/settings/agents): add / rename / reorder / hide agents
- [x] Dashboard PDF + CSV exports for report and metric table (respect current filters)
